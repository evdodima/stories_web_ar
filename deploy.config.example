# Deployment Configuration
# Copy this file to deploy.config and customize for your server

# Server details
DEPLOY_USER="deploy"
DEPLOY_HOST="your-server.example.com"
DEPLOY_PATH="/var/www/webar"

# Optional: SSH port (default: 22)
# DEPLOY_PORT="22"

# Optional: SSH key path
# DEPLOY_KEY="~/.ssh/id_rsa"

# Optional: Exclude patterns (space-separated)
# EXCLUDE_PATTERNS=".git .DS_Store"
